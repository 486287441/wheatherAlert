<Window x:Class="WeatherAlert.TrayPopup.Wpf.Views.HourlyForecastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:WeatherAlert.TrayPopup.Wpf.Converters"
        Title="48 Hour Forecast"
        Width="2280"
        Height="720"
        MinWidth="1440"
        MinHeight="660"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Background="#F5F7FA">
    <Window.Resources>
        <converters:PathToImageSourceConverter x:Key="PathToImageSourceConverter" />
        <DataTemplate x:Key="HourlyForecastItemTemplate">
            <Border Width="76"
                    Margin="0,0,8,0"
                    Padding="4,0"
                    BorderBrush="#E2E8F0"
                    BorderThickness="0,0,1,0">
                <StackPanel HorizontalAlignment="Center">
                    <Image Width="40"
                           Height="40"
                           Margin="0,0,0,6"
                           Source="{Binding IconPath, Converter={StaticResource PathToImageSourceConverter}}"
                           Stretch="Uniform">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasData}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                    <Border Width="40"
                            Height="40"
                            Margin="0,0,0,6"
                            Background="#F8FAFC"
                            CornerRadius="8">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasData}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <TextBlock Text="{Binding TimeLabel}"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#334155"
                               HorizontalAlignment="Center"
                               TextAlignment="Center" />
                    <TextBlock Text="{Binding ConditionText}"
                               FontSize="11"
                               Foreground="#64748B"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               MaxWidth="68"
                               Margin="0,2,0,0" />
                    <TextBlock Text="{Binding DetailText}"
                               FontSize="10"
                               Foreground="#94A3B8"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Margin="0,2,0,0" />
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Border Margin="16"
            Background="#FFFFFF"
            BorderBrush="#E2E8F0"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock x:Name="TitleText"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#1E293B" />
                <TextBlock x:Name="SubtitleText"
                           FontSize="12"
                           Foreground="#64748B"
                           Margin="0,4,0,0" />
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="TodayDayLabel"
                           Grid.Row="0"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#1E293B"
                           Margin="0,0,0,8" />

                <ScrollViewer Grid.Row="1"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled"
                              PanningMode="HorizontalOnly">
                    <ItemsControl x:Name="TodayItems"
                                  ItemTemplate="{StaticResource HourlyForecastItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock x:Name="TomorrowDayLabel"
                           Grid.Row="3"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#1E293B"
                           Margin="0,0,0,8" />

                <ScrollViewer Grid.Row="4"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled"
                              PanningMode="HorizontalOnly">
                    <ItemsControl x:Name="TomorrowItems"
                                  ItemTemplate="{StaticResource HourlyForecastItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock x:Name="EmptyText"
                           Grid.RowSpan="5"
                           Foreground="#64748B"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed" />
            </Grid>

            <Button x:Name="CloseButton"
                    Grid.Row="2"
                    Width="88"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Right"
                    IsCancel="True"
                    Padding="12,6"
                    Background="#F1F5F9"
                    Foreground="#334155"
                    BorderBrush="#CBD5E1"
                    BorderThickness="1"
                    Click="OnCloseClick" />
        </Grid>
    </Border>
</Window>
