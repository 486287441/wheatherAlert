<Window x:Class="WeatherAlert.TrayPopup.Wpf.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="天气提醒历史"
        Width="840"
        Height="460"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WeatherAlert.TrayPopup.Wpf;component/Themes/FrostedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border Style="{StaticResource FrostedCardBorder}" Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Text="历史通知"
                       Style="{StaticResource FrostedTitleText}" />
            <DataGrid x:Name="HistoryGrid"
                      Grid.Row="1"
                      IsReadOnly="True"
                      AutoGenerateColumns="True"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="#E2E8F0"
                      RowBackground="#201E293B"
                      AlternatingRowBackground="#301E293B"
                      HeadersVisibility="Column" />
        </Grid>
    </Border>
</Window>
