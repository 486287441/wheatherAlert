# M06 — 业务迁移（与原项目功能一致）

## 范围

- Core：天气模型、降雨判定、去重抽象
- Infrastructure：和风 API、SQLite、重试
- App：WeatherChecker、WeatherWorker、配置校验、Serilog
- 托盘：立即检查 / 历史 / 切城市 + 左键毛玻璃飞层
- 测试：原 `WeatherAlert.Tests` 对应用例全部迁入

## 验收标准

- [x] `dotnet test` 全绿（含业务 + 飞层模块测试）
- [x] 配置结构与 `src/WeatherAlert.App/appsettings.json` 一致（`Weather` / `Serilog`）
- [x] 支持 `--check-now` 命令行立即检查
- [x] 托盘右键三项 + 左键飞层与原需求一致
